floatconv::fast::u128_to_f32_round:
 push    rbx
 bsr     rax, rdi
 mov     edx, 127
 cmovne  rdx, rax
 xor     rdx, 63
 add     rdx, 64
 bsr     rcx, rsi
 xor     rcx, 63
 test    rsi, rsi
 cmove   rcx, rdx
 mov     rdx, rsi
 shld    rdx, rdi, cl
 mov     rax, rdi
 shl     rax, cl
 xor     r8d, r8d
 test    cl, 64
 cmovne  rdx, rax
 mov     r10d, 0
 cmove   r10, rax
 mov     r9, rdx
 shr     r9, 40
 mov     r11, rdx
 shr     r11, 8
 mov     ebx, edx
 xor     eax, eax
 or      rbx, r10
 setne   al
 or      eax, r11d
 shr     rdx, 39
 mov     ebx, r9d
 not     ebx
 and     ebx, edx
 and     ebx, 1
 sub     eax, ebx
 shr     eax, 31
 shl     ecx, 23
 mov     edx, 1585446912
 sub     edx, ecx
 or      rdi, rsi
 cmove   edx, r8d
 add     edx, r9d
 add     edx, eax
 movd    xmm0, edx
 pop     rbx
 ret
